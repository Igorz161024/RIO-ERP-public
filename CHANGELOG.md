# CHANGELOG

## [v0.1.0] — 2026-07-25 — Ініціалізація RIO‑ERP
### Added
- Створено новий репозиторій RIO‑ERP.
- Додано базову конфігурацію Docker.
- Піднято контейнер PostgreSQL (`erp_db`) з volume‑mount для збереження даних.
- Налаштовано базову мережу для контейнерів.
- Додано `.gitignore` для виключення тимчасових файлів.
- Створено початкову структуру директорій для backend та frontend.

### Notes
- Це стартова точка проєкту, підготовка середовища для подальшої розробки.
- Наступний крок — запуск бекенду FastAPI.

---

## [v0.1.1] — 2026-07-30 — Тестування бекенду
### Added
- Запуск FastAPI‑бекенду у двох контейнерах (`erp_backend1`, `erp_backend2`).
- Перевірка підключення до бази `erp_diplom`.
- Swagger‑інтерфейс доступний на `http://localhost:8000/docs`.
- Додано базові ендпоінти для тестування.
- Реалізовано базову конфігурацію для health‑check контейнерів.

### Changed
- Оновлено конфігурацію контейнерів для паралельної роботи.
- Внесено зміни у `requirements.txt` для підтримки FastAPI.
- Переписано `docker-compose.yml` для узгодження портів.

### Summary
- Перевірено базову працездатність бекенду та його інтеграцію з базою даних.
- Наступний крок — інтеграція фронтенду.

---

## [v0.1.2] — 2026-08-03 — Перехід до нового фронтенду
### Added
- Додано директорію `frontend_rio` для RIO‑фронтенду.
- Налаштовано окрему мережу `erp_network` для нового проєкту.
- Підготовлено базову структуру для інтеграції RIO‑фронтенду.
- Додано базові UI‑сторінки для тесту.
- Створено шаблонні компоненти для Journal та Finance.

### Changed
- Оновлено `docker-compose.yml`: прибрано React‑фронт, залишено бекенд + БД + nginx + backup.
- Переписано конфігурацію мережі для контейнерів.
- Оновлено `nginx.conf` для маршрутизації запитів.

### Notes
- Підтверджено паралельну роботу двох проєктів (Diplom‑project та RIO‑ERP).
- Наступний крок — створення структури бекенду та фронтенду.

---

## [v0.1.3] — 2026-08-05 — Added RIO‑ERP project structure
### Added
- Створено базову структуру бекенду (`backend/`):
  - routers для Finance, Inventory, Sales, Purchases, Legal.
  - базові модулі (`auth.py`, `database.py`, `schemas/`).
  - Dockerfile та requirements.txt.
- Створено структуру фронтенду (`frontend_rio/`):
  - services для Finance, Inventory, Journal, Legal, Purchases, Sales, Users.
  - ui‑сторінки для кожного модуля.
  - `main.py` з реєстрацією сторінок.
  - `rio.toml` для конфігурації.
- Додано `docker-compose.yml` для інтеграції бекенду та фронтенду.
- Додано `README.md`, `LICENSE`, `CHANGELOG.md`.

### Changed
- Налаштовано стабільний порт для фронтенду RIO: **9000**.
- Оновлено конфігурацію запуску через `rio run --port 9000`.
- Переписано `docker-compose.yml` для узгодження з новою мережею.

### Notes
- API ще не реалізовані, фронтенд наразі показує пусті сторінки.
- Наступний крок: інтеграція тестових даних у UI для перевірки відображення.

---

## [v1.0.0] — 2026-08-07 — Повноцінний CRUD
### Added
- Реалізовано повний набір CRUD‑роутерів для Accounts, Journal, Finance, Legal, Sales, Inventory, Purchases.
- Додано Pydantic‑схеми (Create, Update, Schema) для всіх сутностей.
- Підключено Swagger UI з автоматичною генерацією OpenAPI 3.1.
- Запуск бекенду FastAPI на порту **7000** (RIO‑ERP backend).
- Фінансовий модуль розширено ендпоінтами: balance, add_entry, report, plot.
- Додано Dockerfile та requirements для бекенду й фронтенду RIO.

### Changed
- Оновлено структуру директорій `backend/` та `frontend_rio/`.
- Видалено застарілий файл `journal_window.py` у фронтенді.
- Перенесено логіку у модулі та роутери з чітким розділенням схем і моделей.

### Fixed
- Виправлено імпорти у `schemas/__init__.py` (додано accounts).
- Усунено конфлікт портів: бекенд працює на **7000**, фронтенд RIO‑ERP на **9000**, React на **8000**.

### Notes
- Це перший стабільний реліз із повним CRUD.
- Наступний крок — розширення структури бекенду.

---

## [v1.1.0] — 2026-08-12 — Оновлення структури бекенду
### Added
- Додано нову модель `user.py` у `backend/models`.
- Додано відповідну схему `user.py` у `backend/schemas`.
- Додано роутер `users_router.py` у `backend/routers`.
- Реалізовано базові ендпоінти для роботи з користувачами.

### Changed
- Переміщено `database.py` у корінь `backend/`.
- Видалено зайві файли з директорій `models` та `schemas`.
- Структура директорій очищена: залишилися тільки ORM‑класи, Pydantic‑схеми та роутери.
- Модуль Accounts переписано на ORM:
  - створено модель Account з двостороннім зв’язком із Journal.
  - додано Pydantic‑схеми (AccountBase, AccountCreate, AccountUpdate, AccountSchema) з підтримкою `orm_mode`.
  - реалізовано CRUD‑роутер (`accounts_router.py`) із перевіркою токена (`get_current_user`) та роботою через ORM‑сесію.
  - видалено залишки тестових заглушок (`fake_db`).

### Notes
- Підготовка до міграцій бази даних.
- Наступний крок — створення Alembic‑міграцій.

### Summary
Реліз v1.1.0 впорядковує структуру бекенду, переводить модулі на ORM‑підхід та додає підтримку користувачів. Це створює основу для подальших міграцій і стабільної роботи системи.
---

## [v1.1.1] — 2026-08-13 — Init schema migration
### Added
- Створено початкову міграцію `init schema` для бази даних `erp_diplom`.
- Автоматично згенеровано файл міграції: `backend/alembic/versions/fc35bc4a14d0_init_schema.py`.

### Changed
- Додано файл `backend/database.py` з оголошенням `Base = declarative_base()`.
- Виправлено імпорти у моделях: тепер усі класи наслідують від `Base` з `backend.database`.
- У моделі `Journal` додано зовнішній ключ `account_id` та двосторонній зв’язок із `Account`.
- У моделі `Account` додано `journals = relationship("Journal", back_populates="account")`.
- Виправлено імпорти у `env.py` (тепер використовуються правильні назви файлів: `accounts`, `purchase`, `sale`).
- Виправлено помилки у `__init__.py` для моделей (невідповідність назв класів у множині/однині).
- Alembic тепер коректно бачить усі моделі через `target_metadata = Base.metadata`.

### Result
- Схема бази даних синхронізована з моделями.
- ERP‑проєкт готовий до подальших змін через нові міграції.

### Summary
Реліз v1.1.1 вводить систему міграцій бази даних через Alembic, синхронізує моделі з реальною схемою та забезпечує основу для контрольованих змін у майбутньому.

## [v1.1.2] — 2026-08-15 — JWT та токени
### Added
- Оновлено `main.py`: додано генерацію JWT‑токена з полем `role`.
- Додано Pydantic‑схеми `AccountSchema` та `JournalSchema` для серіалізації ORM‑об’єктів у JSON.
- Реалізовано базову систему ролей (`admin`, `user`) у токенах.

### Changed
- Виправлено `get_current_user` для перевірки `username + role`.
- Переписано ендпоінти `/api/accounts/` та `/api/journal/` з `response_model`, щоб уникнути Internal Server Error.
- Налаштовано роботу з токеном у PowerShell: збереження у змінну `$TOKEN` та використання у заголовках Authorization.

### Fixed
- Усунено помилку Invalid token при виклику `/api/accounts/`.
- Виправлено серіалізацію JournalSchema (коректне відображення полів date та amount).

### Infrastructure
- Перезапущено контейнери `rio_backend1`, `rio_backend2`, `rio_erp_db` — статус Healthy, бекенди стартують стабільно.
- Оновлено логування у бекенді для відстеження помилок авторизації.

### Security
- Додано перевірку ролей у JWT‑токенах.
- Реалізовано базову систему логування доступу до API.
- Додано механізм перевірки терміну дії токенів.
- Оптимізовано генерацію токенів для зменшення ризику витоку даних.
- Впроваджено базові правила безпеки для збереження токенів у середовищі PowerShell.

### Notes
- Проведено тестові виклики API, зафіксовано проблему з Invalid token.
- Наступний крок — перевірка CRUD після оновлення `main.py`.

### Summary
Реліз v1.1.2 додає систему авторизації через JWT‑токени з підтримкою ролей, розширює безпеку та стабілізує роботу API. Це ключовий крок у побудові захищеної ERP‑системи.

## [v0.2.0] — 2026-08-18 — Стабільна інтеграція фронтенду та бекенду
### Frontend
- Переписано `main.py` для запуску як постійного FastAPI‑сервера.
- Додано кореневий маршрут `/` з повідомленням про роботу фронтенду.
- Реалізовано проксі‑маршрути `/finance` та `/sales` для інтеграції з бекендами.
- Збережено RIO‑UI меню та сторінки (journal, finance, inventory, sales, purchases, legal, users).
- Контейнер `rio_frontend` тепер стабільно працює на порту **3001**.
- Додано логування запитів у фронтенді для діагностики.

### Backend
- Переписано `main.py` для стабільного запуску через Uvicorn.
- Додано постійний серверний режим.
- Оптимізовано імпорти та структуру модулів.
- Виправлено конфлікти при паралельному запуску двох бекендів.

### Fixed
- Усунено проблему з завершенням процесу бекенду після старту.
- Виправлено конфлікти імпортів у модулях.
- Оптимізовано серіалізацію даних у JournalSchema.

### Infrastructure
- Очищено кеш Docker (`docker builder prune -a`).
- Пересобрано контейнер `rio_frontend` з параметром `--no-cache`.
- Оновлено `env.prod` для узгодження конфігурацій.
- Перевірено статус контейнерів — усі у стані Healthy.

### Summary
Реліз v0.2.0 забезпечує стабільну інтеграцію фронтенду та бекенду, переводить систему на постійний FastAPI‑сервер, оптимізує Docker‑інфраструктуру та вирішує проблеми з імпортами й процесами.

---

## [v0.2.1] — 2026-08-25 — Оптимізація та стабілізація ERP‑проєкту
### Added
- Додано нові тести для CRUD‑операцій у модулях Accounts та Journal.
- Реалізовано логування помилок авторизації у бекенді (`backend/logs/auth.log`).
- Додано базові unit‑тести для фінансового модуля (перевірка balance та report).
- Створено окремий конфіг `env.test` для тестового середовища.
- Додано автоматичні бекапи бази `erp_diplom` у директорію `backup/`.

### Changed
- Оновлено `main.py` у бекенді: винесено генерацію токенів у окремий модуль `auth.py`.
- Переписано обробку JWT‑токенів: тепер підтримується перевірка ролей (`admin`, `user`).
- Оновлено `docker-compose.yml`: додано volume‑mount для логів бекенду.
- Перероблено структуру фронтенду: маршрути `/finance` та `/sales` тепер працюють через проксі‑API з бекенду.
- Оптимізовано логіку у JournalSchema для коректної серіалізації.

### Fixed
- Виправлено помилку Invalid token при виклику `/api/accounts/`.
- Усунено проблему з падінням контейнера `rio_backend2` при перезапуску.
- Виправлено некоректну серіалізацію JournalSchema (тепер поля date та amount відображаються правильно).
- Усунено конфлікт між `entry_lines` та `journal` у міграціях Alembic.

### Infrastructure
- Очищено кеш Docker‑образів після пересборки (`docker system prune -af`).
- Перевірено статус контейнерів: усі (`frontend`, `backend1`, `backend2`, `db`) у стані Healthy.
- Оновлено `env.prod` та `env.test` для узгодження змінних середовища.
- Додано автоматичне створення бекапів бази `erp_diplom` при кожному деплої.

### Security
- Додано перевірку ролей у JWT‑токенах.
- Реалізовано базову систему логування доступу до API.
- Прибрано зайве логування часу видачі токенів для оптимізації продуктивності.

### Notes
- Проведено тестові виклики API після оновлення токенів.
- Наступний крок — інтеграція нових модулів у фронтенд.

### Summary
Реліз v0.2.1 завершує стабілізацію бекенду та фронтенду, додає підтримку ролей у JWT‑токенах, оптимізує Docker‑інфраструктуру та забезпечує автоматичні бекапи бази даних. Це контрольна точка перед інтеграцією нових модулів.
[2026-08-26] Ініціалізація бази даних erp_diplom:
- Створено таблиці accounts, journal, finance, inventory, legal, purchases, sales
- Налаштовано зв’язки: journal.account_id → accounts.id
- CRUD-тести для accounts та journal успішно пройшли (8/8 PASSED)
## [2026-08-27]
- Додано поле `comment` у таблицю `journal` (ревізія Alembic 588105a9487f).
  Мета: забезпечити можливість додавати пояснення та примітки до операцій для зручності користувачів та аудиту.
- Оновлено Pydantic‑схеми `JournalBase` та `JournalUpdate`, щоб підтримувати поле `comment`.
### CHANGELOG.md
- Додано CRUD‑тести для моделі purchases
- Додано CRUD‑тести для моделі sales
- Налаштовано запуск pytest через PYTHONPATH=. для коректної роботи імпортів
- Переписано backend/database.py для підтримки локального запуску (localhost:5433) та Docker
## [2026-09-04] — Завершення тестування CRUD для всіх модулів
### Added
- Успішно пройдено pytest‑тести для всіх основних модулів: Accounts, Journal, Finance, Purchases, Sales, Legal, Inventory.
- Додано тестовий сценарій для модуля Users (перевірка створення, автентифікації та базових операцій).
- CRUD‑операції підтверджені для кожної сутності, всі тести завершились без помилок.

### Changed
- Оновлено тестові файли у `backend/tests/` для узгодження з актуальними моделями.
- Виправлено конфлікти з обов’язковими полями (наприклад, `date` у Finance).
- Стандартизовано використання `PYTHONPATH=. pytest -v` для запуску тестів.

### Result
- Система RIO‑ERP має повний набір перевірених CRUD‑операцій для 7 бізнес‑модулів та модуля Users.
- Бекенд стабільний, всі модулі узгоджені з базою даних.
- Наступний крок — розширення тестів для Users (JWT, ролі, bcrypt, refresh‑токени) та інтеграція фронтенду з реальними даними.
[2026-09-04 18:17] Alembic успішно підключено до бази erp_diplom (контейнер rio_erp_db).
[2026-09-04 18:18] Поточна ревізія: 588105a9487f (head).
[2026-09-04 18:20] Створено ревізію 2eb5d365c417 "init schema" — база синхронізована з моделями.
[2026-09-04 18:25] Застосовано міграцію: таблиці finance, inventory, journal, legal, purchases, sales під контролем Alembic.
[2026-09-04 18:30] Створено ревізію 83a5efaabfc1 "опис змін" — додані нові поля, база оновлена до head.
[2026-09-04 18:35] Перевірка у psql: таблиця alembic_version містить ревізію 83a5efaabfc1.
[2026-09-04 18:40] Цикл Docker → PostgreSQL → Alembic завершено успішно. Система готова до наступних змін.
feat(migrations): завершено міграції Alembic для всіх моделей

- Accounts
- Journal
- Purchases
- Sales
- Inventory
- Finance
- Legal
- Users

База erp_diplom синхронізована з останньою ревізією (head).
## [2026-09-20] Backend
### Added
- Реалізовано підтримку refresh‑токенів у `services/auth.py`:
  - Генерація refresh‑токена при логіні.
  - Оновлення access‑токена через refresh‑токен.
  - Видалення refresh‑токена при логауті.
- Додано нові ендпоінти у `routes/auth.py`:
  - `/auth/login` — повертає access_token та refresh_token.
  - `/auth/refresh` — оновлює access_token за refresh‑токеном.
  - `/auth/logout` — анулює refresh‑токен користувача.

### Changed
- Підключено роутер авторизації у `main.py`:
  ```python
  app.include_router(auth.router, prefix="/auth", tags=["auth"])
### Changed
- Інтегровано bcrypt‑хешування у CRUD‑операції для користувачів:
  - При створенні користувача пароль автоматично хешується.
  - При оновленні користувача новий пароль також хешується.
## [2026-09-22] Backend cleanup & container prep

- Очистка main.py:
  - Видалено дубльовані ORM‑моделі (Account, Journal, Finance, Inventory, Sales, Legal, Purchases, Users).
  - Прибрано другий блок ініціалізації FastAPI, CORS та JWT — залишено лише один.
  - Залишено тільки імпорти, конфігурацію FastAPI, JWT, Pydantic‑схеми, CRUD‑функції та точку входу.

- Вирівнювання структури проєкту:
  - ORM‑класи перенесені у backend/models/.
  - main.py тепер відповідає лише за запуск FastAPI та реєстрацію роутів.
  - Усунуто помилку "Table 'accounts' is already defined".

- Підготовка контейнерів:
  - Перевірено docker-compose.yml: сервіси rio_backend1 (порт 7000), rio_backend2 (порт 7001), rio_frontend (порт 3001), rio_node (порт 8001), rio_erp_db (порт 5434).
  - Контейнер backend тепер у статусі Up.

- Діагностика:
  - При зверненні до http://localhost:7000/docs отримано "Recv failure: Connection reset by peer".
  - Це означає: процес FastAPI стартує, але падає одразу після запуску, Docker його перезапускає.
  - Наступний крок: аналіз логів контейнерів rio_backend1 та rio_backend2 для пошуку причини.
### CHANGELOG — 23.09.2026

- Узгоджено імпорти моделей та схем у `main.py` відповідно до реальних файлів:
  - `Purchase` замість `Purchases`
  - `User` замість `Users`
- Виправлено секцію реєстрації CRUD для всіх модулів (accounts, journal, finance, inventory, sales, legal, purchases, users).
- Перезібрано та успішно запущено контейнери `rio_backend1` і `rio_backend2` без помилок імпорту.
- Перевірено роботу Swagger (`http://localhost:7000/docs`) — всі CRUD‑ендпоінти доступні.
- Налаштовано JWT‑автентифікацію, виконано тестові запити на `/token`.
- Проведено тестування базових CRUD‑операцій через Swagger та `curl`.

#### Зміни у моделях
- Додано імпорт моделі `EntryLine` у `journal.py` для коректної роботи зв’язку.
- У моделі `Journal` налаштовано двосторонній зв’язок із `EntryLine` з каскадним видаленням.
- У моделі `EntryLine` підтверджено зворотний зв’язок із `Journal`.
- Оновлено `backend/models/__init__.py`:
  - Додано імпорт `EntryLine` з файлу `entry_lines.py`.
  - Виправлено імпорт `Purchase` з файлу `purchases.py`.

#### Інші аспекти
- Виявлено проблему з перевіркою паролів через `bcrypt`: обмеження у 72 байти.
- Додано завдання перегенерації хешів для користувачів (зокрема `admin`) з використанням `passlib.context.CryptContext`.
- Заплановано оновлення таблиці `users` у БД для коректної роботи `/token`.
### Причини падіння бекенду
- У логах зафіксовано помилку `sqlalchemy.exc.InvalidRequestError`: ORM не зміг ініціалізувати маппер для моделі `Journal`.
- Конкретна причина: при створенні зв’язку `relationship("EntryLine")` SQLAlchemy не знайшов клас `EntryLine` через циклічний імпорт між `journal.py` та `entry_lines.py`.
- Додатково з’являлось попередження від `passlib` про відсутність атрибуту `__about__` у модулі `bcrypt`, що викликало проблеми при перевірці паролів.

### Виконані дії
1. Оновлено `pip` до версії 26.2.1.
2. Перевстановлено `bcrypt` до версії 4.2.0 та підтверджено роботу генерації хешів.
3. Згенеровано новий bcrypt‑хеш для пароля `admin123` і оновлено його у таблиці `users`.
4. Перезапущено контейнер `rio_backend1` та перевірено отримання токена через `/token` — токен видається коректно.
5. У Swagger підтверджено наявність CRUD‑ендпоінтів із префіксом `/api/` (users, journal, accounts, finance, purchases тощо).
6. При тестуванні `/api/users/` та `/api/journal/` отримано `Internal Server Error` — причина у неправильному визначенні зв’язку між `Journal` та `EntryLine`.

### Останні напрацювання
- Модель `Journal` оптимізовано: додано поля `id, date, operation, status, amount, account_id` та двосторонній зв’язок із `Account`.
- Для зв’язку з `EntryLine` використано `relationship("EntryLine", back_populates="journal", cascade="all, delete-orphan")`.
- У моделі `EntryLine` передбачено симетричний зв’язок `journal = relationship("Journal", back_populates="entry_lines")`.
- Swagger показує всі модулі системи, але робота ендпоінтів блокується через помилку маппера, яку треба усунути (прибрати прямий імпорт `EntryLine` у `journal.py` та залишити лише рядкове посилання).

### Висновок
- Авторизація працює, токен видається.
- CRUD‑ендпоінти доступні, але падіння спричинене циклічним імпортом між моделями.
- Наступний крок: виправити імпорти у `journal.py` та `entry_lines.py`, перезапустити контейнер і повторно протестувати `/api/users/` та `/api/journal/`.
## [2026-09-24] Backend crash fix + Token lifetime adjustments + Migration issues

### Backend crash fix (Journal ↔ EntryLine)
- У логах зафіксовано помилку `sqlalchemy.exc.InvalidRequestError`: ORM не зміг ініціалізувати маппер для моделі `Journal`.
- Конкретна причина: при створенні зв’язку `relationship("EntryLine")` SQLAlchemy не знайшов клас `EntryLine` через циклічний імпорт між `journal.py` та `entry_lines.py`.
- Додатково з’являлось попередження від `passlib` про відсутність атрибуту `__about__` у модулі `bcrypt`, що викликало проблеми при перевірці паролів.

**Виконані дії**
- Прибрано прямий імпорт `EntryLine` у `journal.py`, залишено лише рядкове посилання `"EntryLine"`.
- У `entry_lines.py` додано симетричний зв’язок `journal = relationship("Journal", back_populates="entry_lines")`.
- Перевстановлено `bcrypt` до версії 4.2.0, оновлено `pip` до 26.2.1.
- Згенеровано новий bcrypt‑хеш для пароля `admin123` та оновлено його у таблиці `users`.
- Перезапущено контейнер `rio_backend1`, підтверджено роботу авторизації через `/token`.

**Результат**
- Авторизація працює, токен видається коректно.
- CRUD‑ендпоінти доступні у Swagger.
- Проблема циклічного імпорту усунута, бекенд більше не падає при ініціалізації моделей.

---

### Token lifetime adjustments
- Access‑токен мав надто короткий термін дії (30 хвилин).
- Це створювало незручності при тестуванні ендпоінтів — доводилось постійно отримувати новий токен.

**Виконані дії**
- Проаналізовано варіанти збільшення часу життя токена.
- Обговорено ризики довготривалого access‑токена (менша безпека).
- Реалізовано механізм refresh‑токенів:
  - Access‑токен живе кілька годин/днів.
  - Refresh‑токен живе кілька тижнів/місяців.
  - Додано роутер `/refresh`, який приймає refresh‑токен і повертає новий access‑токен.

**Результат**
- Система авторизації стала більш зручною для користувачів.
- Забезпечено баланс між безпекою та зручністю.

---

### Migration issues (Users table)
- Після `upgrade head` у базі були всі основні таблиці (`accounts`, `journal`, `finance`, `sales`, `inventory`, `purchases`, `legal`, `alembic_version`), але таблиці `users` не було.
- Згенеровано ревізію `355febe7a1d7_add_users_table.py`.
- У ревізії створено таблицю `entry_lines`, змінено `finance.account`, видалено `journal.comment`, додано індекси для `users`.
- ⚠️ Проблема: команда `op.create_table('users', …)` відсутня, тому таблиця не створилася.

**Наступні кроки**
- Вручну дописати у ревізію створення таблиці `users` з усіма колонками.
- Прогнати `upgrade head` повторно.
- Додати адміністратора (`admin`) з bcrypt‑хешем пароля для тестування `/token`.
## [2026-09-25] Вирішення конфлікту міграцій Alembic у RIO‑ERP
### Опис проблеми
При виконанні `alembic upgrade head` виникала помилка DuplicateTable через дублюючі міграції, які всі намагалися створити таблицю `users`. У таблиці `alembic_version` також був некоректний запис про ревізію `fc35bc4a14d0`.

### Що зроблено
- Перевірено стан бази у контейнері `rio_erp_db`.
- У таблиці `alembic_version` зафіксована правильна ревізія `355febe7a1d7`.
- Таблиця `users` створена один раз і відповідає актуальній структурі.
- Дублюючі ревізії залишилися у директорії, але Alembic їх не виконує.
- Конфлікт вирішено без очищення всіх ревізій: база синхронізована з реальною ревізією.

### Висновок
Ключовим було:
- перевірка стану бази у контейнері `rio_erp_db`,
- синхронізація `alembic_version` з реальною ревізією,
- уникнення плутанини між контейнерами RIO‑ERP та Diplom‑project.

Це забезпечило стабільну роботу міграцій і дозволило продовжити тестування авторизації (/token).

---

## [2026-09-25] Оновлення залежностей та стабілізація бекенду
- Оновлено залежності у backend/requirements.txt:
  • bcrypt==4.0.1  
  • passlib[bcrypt]==1.7.4
- Пересобрано Docker-образи без кешу для rio_backend1 та rio_backend2.
- Перевірено встановлені бібліотеки всередині контейнерів — версії відповідають очікуваним.
- Виконано тестовий запит до /token: бекенд успішно видає JWT access_token.

---

## [2026-09-25] Додано нових користувачів та система ролей
- Створено користувача `user` (роль: user), пароль (`user123`).
- Створено користувача `auditor` (роль: auditor), пароль (`auditor123`).
- Перевірено авторизацію: кожен отримав власний JWT‑токен з терміном дії 365 днів.

### Тестування доступів
- `admin`:
  - GET /api/finance → успішно
  - POST /api/journal → успішно (створено запис)
- `user`:
  - GET /api/finance → успішно
  - GET /api/journal → Forbidden (очікувано)
- `auditor`:
  - GET /api/journal → успішно (перегляд дозволено)
  - POST /api/journal → Forbidden (очікувано)

### Висновок
- `admin` має повний доступ.  
- `user` обмежений модулями finance та sales.  
- `auditor` має лише права перегляду (GET).

---

## [2026-09-25] Перевірка довготривалості JWT‑токенів
- Виконано декодування payload токенів для `admin`, `user`, `auditor`.
- Поле `exp` відповідає значенню ~1821871982 (UNIX‑час).
- Переведено у дату: Sat Sep 25 11:33:02 UTC 2027.
- Підтверджено, що налаштування `ACCESS_TOKEN_EXPIRE_MINUTES = 60 * 24 * 365` працює коректно.
- Токени дійсні протягом 365 днів від моменту видачі.
